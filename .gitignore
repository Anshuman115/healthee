# secrets & env
.env
.env.*
!.env.example
.secrets/
*.pem
*.key

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# flutter / dart
apps/mobile/.dart_tool/
apps/mobile/build/
apps/mobile/.flutter-plugins*
apps/mobile/android/.gradle/
apps/mobile/android/local.properties
apps/mobile/android/key.properties
# The signing key itself, wherever it is put. It is the update channel: an APK
# signed by a different key cannot update an install, so this must never leak and
# must never be lost.
*.jks
*.keystore
*.apk

# node (contracts/knowledge tooling, if any)
node_modules/

# data & scratch
/data/
*.sqlite
*.db
scratch/
.turbo/
*.tsbuildinfo

# os / editor
.DS_Store
.idea/
.vscode/
apps/mobile/build.env
